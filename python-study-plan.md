# Python 基础语法 · 四课学习方案

> 目的：用 4 次课（每课 2 × 45 分钟，共 360 分钟）掌握 Python 基础语法，能用 Python 完成小型统计分析任务。
> 学习起点：2026-09-16（假定零基础或仅有少量接触）。

## 三种方案对比

| 维度 | 方案 A · 数据分析导向（推荐） | 方案 B · 经典语法扎实路线 | 方案 C · 项目驱动路线 |
|------|------|------|------|
| 思路 | 围绕"读数 → 算 → 出图"裁剪语法，少学够用 | 把 Python 核心语法系统过一遍，重底层 | 一个项目从头做到尾，边做边学 |
| 终点能力 | 能用标准库 + pandas 做描述统计 | 理解 Python 模型与大多数语法特性 | 完成 1 个端到端项目 |
| 优势 | 与目标（统计分析）高度匹配，不浪费时间 | 长期收益高，后续学什么都快 | 动机强、记得牢 |
| 风险 | 部分主题跳过（如 OOP、装饰器），后续需要时再补 | 前 2 课枯燥、与"统计"目标距离远 | 进度不易控制，遇到难题容易卡 |
| 适合 | 目标明确是"用 Python 做统计" | 想扎实学编程、打算长期使用 Python | 学习需要外部反馈/成就感 |

**推荐方案 A**——你已说明"后续用 Python 做统计分析"，方案 A 最快把这条路打通；缺什么后续单独补即可。

---

## 方案 A · 数据分析导向（推荐）

### 第 1 课 · 入门语法

**上半 45 分钟**
- 安装 Python 与编辑器（推荐系统自带 3.13.12 或 3.12.10；编辑器用 VS Code 或记事器）
- 第一个程序：`print("hello")`、`input()`
- 变量与基本类型：`int / float / str / bool / None`
- 算术运算（`+ - * / // % **`）与赋值
- 字符串与 f-string：`f"你好，{name}，今年 {age} 岁"`
- 数据类型转换：`int() / float() / str()`

**下半 45 分钟**
- 容器：`list / tuple / dict / set` 各自特点与选用场景
- 索引、切片：`nums[0]`、`nums[-1]`、`nums[1:3]`
- 增删改查：`.append / .insert / .remove / .pop / .update`
- `for ... in` 遍历；`in / not in` 成员检查；`len()`
- 列表推导式入门：`[x*2 for x in nums]`
- **课后练习**：BMI 计算器（输入身高体重 → 输出 BMI 与分类）

### 第 2 课 · 流程控制

**上半 45 分钟**
- 布尔表达式：`== != > < >= <=`
- 逻辑运算：`and / or / not`，短路求值
- `if / elif / else` 多分支
- 三元表达式 `x if cond else y`
- `assert` 调试小技巧

**下半 45 分钟**
- `while` 循环与 `for` 循环的选用
- `range(start, stop, step)`
- `break / continue / else 子句`
- `enumerate()`：带索引遍历
- 列表推导式进阶：`[x for x in nums if x > 0]`
- 字典推导式：`{k: v for k, v in pairs}`
- **课后练习**：把一份成绩列表按分数段分类输出（及格/良好/优秀）

### 第 3 课 · 函数与模块

**上半 45 分钟**
- `def` 定义函数；`return` 多值
- 参数：位置参数 / 默认参数 / 关键字参数
- 文档字符串 `"""..."""` 与类型提示 `def f(x: int) -> int:`
- 作用域：局部 vs 全局；`global` 简介（慎用）
- 把上节课的"成绩分类"改写成函数

**下半 45 分钟**
- `import` 模块：`import math` / `from math import sqrt` / `import math as m`
- 标准库速览：`math / random / statistics / os.path`
- 用 `statistics` 实现：均值 / 中位数 / 标准差
- `pip` 安装第三方库（命令行，隔离环境）
- **NumPy 入门预览**：`np.array([1,2,3])`、向量化运算
- **课后练习**：写一个 `def describe(nums)` 函数，返回均值/中位数/标准差/最值

### 第 4 课 · 数据读写与项目实战

**上半 45 分钟**
- 文本文件读写：`open(path, 'r')` / `with open(...) as f`
- CSV 处理（标准库 `csv` 模块）：`csv.reader / csv.DictReader`
- 异常处理：`try / except / else / finally`，常见异常类型
- `with` 上下文管理器（自动关闭文件）
- `json` 模块简介：`json.load / json.dump`

**下半 45 分钟**
- **pandas 入门**：
  - `Series`、`DataFrame` 的概念
  - `pd.read_csv()` 读数据
  - `df.head() / df.info() / df.describe()`
  - 选列、过滤：`df[df['score'] >= 60]`
  - 分组聚合：`df.groupby('class')['score'].mean()`
- **matplotlib 入门**：`df['score'].hist()` 一行出图
- **项目实战**：拿到一份"学生成绩.csv"
  1. 读入 → 2. 清洗（去空值/异常值）→ 3. 描述统计 → 4. 按班级对比 → 5. 出柱状图 → 6. 导出汇总表
- **课后总练习**：完成上面的项目，下次课分享

---

## 方案 B · 经典语法扎实路线（备选）

适合想长期用 Python、希望理解更深入的人。

- **第 1 课**：变量、类型、控制流（与方案 A 第 1+2 课合并）
- **第 2 课**：函数深入（`*args / **kwargs / 闭包 / lambda`）、装饰器概念
- **第 3 课**：面向对象（`class` / `__init__` / 继承 / 魔术方法 / `@dataclass`）
- **第 4 课**：模块化（包 / `__init__.py`）、`pip`、虚拟环境、测试（`pytest`）、Type Hints、`typing`

> 前 2 课基本学不到任何"统计"相关工具；统计部分要在后续课程补充 pandas/numpy，节奏比方案 A 慢一倍以上。

---

## 方案 C · 项目驱动路线（备选）

围绕 **"学生成绩分析"** 这一真实项目展开：

- **第 1 课**：用 `print/input` 录入 3 个学生成绩 → 算总分与平均 → 打印。学到的语法是变量、输入输出、算术。
- **第 2 课**：加判断（及格/不及格），加循环（录入 10 个学生）。学到的语法是 `if / for / list`。
- **第 3 课**：把第 1 课代码封装成函数；用字典存学生信息。学到的语法是 `def / dict`。
- **第 4 课**：用 pandas 把同一份数据再处理一遍，输出对比柱状图。学到的语法是 `import`、基本 DataFrame。

> 优点：每一行代码都服务于同一个目标，成就感强。缺点：语法知识是"散点式"学的，缺少体系；遇到难题（如异常处理、文件路径）容易被卡住。

---

## 学习节奏建议（适用于方案 A）

- **课前 10 分钟**：复盘上节课作业，挑 1–2 个典型错误当堂讲解
- **课中**：每段知识点讲 15 分钟 → 当堂练习 20 分钟 → 答疑 10 分钟
- **课后作业**：每节课 30 分钟左右
- **间隔**：建议每周 1–2 课，给消化与作业留时间
- **总练习（最后一节布置）**：用 pandas 完成"学生成绩.csv"的描述统计与对比图

## 工具与环境

- **Python**：建议用工作区已配置的解释器（3.13.12 或 3.12.10）
- **编辑器**：VS Code + Python 扩展（强烈推荐），或任意文本编辑器
- **第三方库安装**：第 4 课前提前装好 `pandas / numpy / matplotlib`，命令：
  ```bash
  pip install pandas numpy matplotlib
  ```
- **推荐练习数据集**：公开的 "Titanic" 或鸢尾花数据集（少量、易上手）

## 学完之后的衔接方向

按方案 A 完成后，下一阶段可自然衔接：
1. **NumPy 进阶**：广播、轴运算、随机数
2. **pandas 进阶**：缺失值处理、合并表、时间序列
3. **统计可视化**：matplotlib 调样式、seaborn 高级图
4. **描述统计与推断统计**：`scipy.stats`、假设检验入门