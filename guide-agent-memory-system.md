---
title: "Agent 记忆系统 · Guide Mode 讲解"
type: guide
tags: [guide-mode, agent, memory, llm-wiki]
date: 2026-09-16
source_concept: agent-memory-system
---

# Agent 记忆系统 · Guide Mode

> 本文档由 `learn-studio` Skill 按 guide 模式生成。第一节讲通用理论，第二节起落到具体设计。建议顺序阅读。

---

## 01 它是什么

**Agent 记忆系统**是让大模型突破上下文窗口限制、在多次会话中保持一致性的整套机制——既包括临时塞进提示词的**短期记忆**，也包括写入向量数据库或键值库的**长期事实**。

四个最基础的子概念（后文逐节展开）：

- **为什么需要记忆**：上下文窗口有限 + 跨会话一致性需求
- **短期 vs 长期**：短期=当前会话对话历史；长期=用户偏好、历史结论、个性化事实
- **三种记忆类型**：情景（具体事件）/ 语义（概念知识）/ 程序性（操作步骤）
- **遗忘机制**：过期、冲突、噪声的清理策略

简言之：**记忆是把"用户用 LLM 的体验"从一次性对话升级为长期关系的关键基础设施**。

---

## 02 核心设计

### 五大核心要素

| 要素 | 它是什么 | 解决什么问题 |
|------|----------|--------------|
| **写入触发器** | 决定什么时候把信息记下来 | 避免漏记关键事实 / 避免记太多噪声 |
| **抽取器** | 从对话/工具结果识别"值得记的内容" | 把非结构化文本变成结构化事实 |
| **分类器** | 把事实分到事实型/事件型/语义型/程序型 | 让检索按需匹配 |
| **存储层** | 选 SQL / 向量库 / 队列 | 决定读写的速度与查询能力 |
| **遗忘策略** | 过期清理、冲突解决、噪声过滤 | 控制记忆库规模与质量 |

### 与通用理论的对应

- 写入触发器 ≈ 数据库的"日志写入策略"（同步/异步）
- 抽取器 ≈ ETL（Extract-Transform-Load）的 E 步
- 分类器 ≈ 索引设计
- 存储层 ≈ 表结构选择
- 遗忘策略 ≈ GC（垃圾回收）

记忆系统的设计本质上是"为非结构化场景做结构化工作"。

---

## 03 运行主线

```
用户提问 → ① 检索相关记忆 → ② 与对话历史拼成 prompt
                                   ↓
                            模型生成回答
                                   ↓
              ④ 抽取值得记的事实 → ⑤ 分类 → ⑥ 写入存储
                                   ↓
                            ⑦ 必要时遗忘旧条目
```

### 关键判断点

- ① **检索时机**：每轮都查，还是只在 LLM 主动要求时查？大多数产品每轮都查，保证上下文稳定
- ② **拼装顺序**：检索片段放最前（强化）、对话历史居中、系统提示词最末
- ④ **抽取触发**：每轮结束都抽 vs 标记关键对话才抽？前者简单但噪声大，后者省事但漏记多
- ⑦ **遗忘时机**：写入时检测冲突 / 定期清理过期 / 用户主动删除

---

## 04 WorkBuddy 记忆设计

WorkBuddy 把记忆分为**三层**，对应不同的存储与生命周期：

### 三层架构

| 层级 | 内容 | 存储 | 生命周期 |
|------|------|------|----------|
| **L1 会话层** | 当前会话上下文、工具调用结果 | 内存 / prompt | 会话结束即丢 |
| **L2 项目层** | 项目约定、本次任务的偏好、用户给定的角色 | 临时文件 / KV | 任务结束清理 |
| **L3 跨会话层** | 用户长期偏好、过往结论 | 向量库 + 文件 | 持久，按遗忘策略淘汰 |

### 与通用理论的对应

- L1 = 短期记忆（Working Memory）
- L2 = 程序性记忆（Procedure）+ 当前项目的事实型记忆
- L3 = 长期记忆（Long-term Memory）的全部三类（情景/语义/程序）

### 工程实现

- **L3 抽取**：每会话结束自动跑抽取器，结果写入 `~/.workbuddy/memory/<date>.md`
- **L3 检索**：每次新会话开始读最近 30 天 memory，匹配 query 后回填
- **遗忘**：30 天前的工作日志由 `MEMORY.md` 提炼摘要，原日志删除（详见 05 铁律）

---

## 05 铁律

1. **必须区分短期与长期**：把对话历史塞进 L3 等于把噪声全归档，检索命中率骤降
2. **禁止无差别抽取所有对话**：必须用触发条件（用户明确确认 / 模型主动标 / 关键事件节点）
3. **永远给记忆设过期**：不遗忘的记忆库半年内会塞满噪声，效率断崖式下降
4. **写入时必须检测冲突**：用户说"我改主意了"时，旧记忆不能继续生效
5. **检索片段必须有溯源**：用户问到时必须能回指原始会话或文档，否则不可信

违反任何一条，记忆系统都会从"基础设施"退化为"数据坟墓"。

---

## 06 溯源

| 来源 | 类型 | 用在 |
|------|------|------|
| [Lilian Weng: LLM Powered Autonomous Agents](https://lilianweng.github.io/posts/2023-06-23-agent/) | 综述博客 | 01、02（记忆分类、Memory Stream 概念） |
| [Anthropic: Effective context engineering](https://www.anthropic.com/news/context-engineering) | 官方博客 | 02、03（上下文工程思想、agent 落点） |
| [Mem0 Research](https://mem0.ai/research) | 工程实践 | 04、05（生产级 Memory System 设计参考） |
| [LangGraph Memory Docs](https://docs.langchain.com/oss/python/langgraph/memory) | 官方文档 | 02、03（短期/长期记忆的工程实现） |
| `llm-wiki-agent/wiki/sources/agent-memory-system.md` | 本仓库 wiki | 04、05（WorkBuddy 三层设计的本地资料） |

---

## 07 自测

<details>
<summary><strong>1. 短期记忆和长期记忆的本质区别是什么？</strong></summary>

短期记忆是"当前会话对话历史"，直接拼进 prompt；长期记忆是"用户偏好、过往结论、个性化事实"，需要经过抽取/分类/写入/检索四步才能用。本质区别在于**写入路径和生命周期**。
</details>

<details>
<summary><strong>2. 记忆系统与 RAG 的核心差异是什么？</strong></summary>

RAG 从**外部文档库**检索已知知识；记忆从"关于用户/自身的事实库"检索个性化信息。RAG 解决"知识广度"问题，记忆解决"关系延续"问题。
</details>

<details>
<summary><strong>3. 为什么"记忆越多越好"是错的？</strong></summary>

记忆会带来噪声与无关信息，反而干扰判断。工程要点是**取舍**——值得记什么、什么时候记、什么时候忘。设了过期机制的记忆库，比无脑归档的更有用。
</details>

<details>
<summary><strong>4. WorkBuddy 的 L3 层为什么拆成"事实/事件/语义/程序"四类？</strong></summary>

因为检索时不同问题需要不同结构的事实——查"用户偏好"是事实型，查"上周聊过什么"是事件型，查"某概念定义"是语义型，查"某流程怎么走"是程序型。分类让检索可以按需匹配。
</details>

<details>
<summary><strong>5. 给出一种"违反 05 铁律第 3 条"的反例。</strong></summary>

把所有历史对话无差别写入 L3，半年后用户问"我三个月前让你帮我做的项目进展如何"，检索会返回一堆无关闲聊片段，模型要么答错要么放弃——这就是"数据坟墓"。
</details>

---

_本文档由 `learn-studio` Skill 按 guide 模式生成，已人工核查。检索日期：2026-09-16。_